import PageHeader from "@/components/pageHeader";

type Props = object

const AcceptedAnswer = ({ }: Props) => {
  return (
<div className="">
    <PageHeader title="채택된 답변"/>
    </div>
  );
};

export default AcceptedAnswer;