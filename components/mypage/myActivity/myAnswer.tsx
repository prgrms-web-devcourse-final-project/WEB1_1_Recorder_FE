import PageHeader from "@/components/pageHeader";

type Props = object

const myAnswer = ({ }: Props) => {
  return (
<div className="">
    <PageHeader title="나의 답변"/>
    </div>
  );
};

export default myAnswer;